# 使用legacy peer deps模式，避免peer dependency问题
legacy-peer-deps=true

# 禁用funding消息
fund=false

# 禁用audit（减少构建时间）
audit=false

# 设置网络超时
fetch-retries=3
fetch-timeout=120000

# 设置缓存设置
prefer-offline=true

# 禁用进度条，减少日志
progress=false